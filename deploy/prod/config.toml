[http]
enabled = true
port = 8080
bind = "0.0.0.0"

# Replace this with your company VPN / Tailscale CIDR so explorer admin writes
# are allowed only from trusted networks. With an empty list, only loopback is trusted.
trusted_cidrs = ["100.64.0.0/10"]
kaspa_explorer_base_url = "https://kaspa.stream"

[prometheus]
enabled = true
port = 9090

[[chains]]
name = "igra"
chain_id = 38833
rpc_url = "https://rpc.igralabs.com:8545"
pg_url = "postgres://tidx@postgres:5432/tidx_igra"
pg_password_env = "POSTGRES_PASSWORD"
backfill = true
backfill_first = false
batch_size = 100
head_delay_blocks = 30
max_head_delay_blocks = 100
head_delay_window_secs = 600
concurrency = 6

# Keep false unless your devops team decides the RPC should be treated as authoritative.
trust_rpc = false

[chains.clickhouse]
enabled = true
url = "http://clickhouse:8123"
database = "tidx_38833"

[chains.kaspa]
enabled = true
rpc_url = "ws://host.docker.internal:17111"
txid_prefix = "97b1"
poll_interval_ms = 1000
initial_tip_distance = 100
promotion_delay_secs = 43200
